import React from "react";

interface ArtistFormProps {
  onCreated?: () => void;
}

// Temporarily simplified to avoid build-time errors. Admin form will be re-enabled later.
const ArtistForm: React.FC<ArtistFormProps> = () => {
  return null;
};

export default ArtistForm;
